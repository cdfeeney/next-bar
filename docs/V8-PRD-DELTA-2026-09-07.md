# V8 contract 3.1.1 - recommendation correction, 2026-09-07

Status: founder-approved, 2026-09-07.

Approved by the operator in the Codex conversation: the three-item overnight backlog, followed by approval of the prepared execution backlog and instruction to start executing until 2026-09-08 08:00 America/New_York. The prior 3.1.0 contract and its historical decisions remain in force except for the explicit overrides below. This is capability and related displayed-copy approval, not a new visual redesign.

## V8-R-NXT-008 / D-C-40: travel selection recomputes results

Walkable, Worth a cab and Anywhere must recompute the intended eligible/ranked pool and routes when selected. Late responses from previous selections must not replace current results. Prove changed result IDs with controlled data where the intended pool differs; do not force arbitrary changes where correct results overlap. Preserve existing route and distance semantics, map behavior and Places UI. Empty/unavailable routes need honest states. Both mobile viewports and rapid selection changes require coverage.

## V8-R-NXT-009 / D-C-41: explicit vibes determine eligibility and badge

Without an active explicit selection, omit the Vibe match badge. With N distinct selected vibes, use N as denominator and selected-tag intersection as numerator. A bar is eligible only with at least max(1, N-1) matches: allow 1/2, 3/4, 5/6; reject 0/1 and any result missing two or more choices. Apply the rule before candidate truncation and to all fallback/refresh/supplement paths. Return fewer results or an honest empty state rather than padding with rejected bars. Clearing the selection removes this gate and badge. Saved quiz tags remain a cold-start prior, never an implicit explicit selection. Stable card selectors must replace selectors that depend on visible Vibe match text, preserving the no-saved-profile-leak invariant.

This overrides earlier nonmatching fill behavior for explicitly applied vibes, including the general five-result rule where fewer eligible bars exist. It does not introduce score-based exclusion or change the learned-taste model.

## V9 and release boundary

A separate V9 worktree initially audits legacy code and specifies bounded retirement/refactor slices with callers, measurable costs and verification. Preserve user data and approved behavior. A whole-codebase rewrite is not this run's completion criterion. V8 push is conditionally authorized after exact-candidate release checks; the overnight workers themselves only produce local candidates.

## Admission repair evidence

Commit c08408f4bc5a4ad3d6aec5bcf72d0efd1860e744 added JSDoc argument types to the validator and adjusted TypeScript fixture annotations. Inspection found no runtime validator-logic change. The 69 validator tests passed on 2026-09-07; log D:/Temp/nb-overnight-contract-tests-20260907.log. Contract 3.1.1 records the current normalized hashes without changing validator behavior.
