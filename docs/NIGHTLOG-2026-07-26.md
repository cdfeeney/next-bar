# Night Log — 2026-07-26 (overnight loop)

Goal `g-1a91cb5a-14a3-4418-8148-51cf8786e74c`. One 🟢 step per tick. Keep the build green.
Escalate-don't-fake. **Never push.**

---

## ⚠️ READ FIRST — this loop runs in a WORKTREE, not the main checkout

**Working directory: `C:\Users\cdfee\projects\next-bar-share`** (branch `feat/share-loop-week1`).

`C:\Users\cdfee\projects\next-bar` is a DIFFERENT session's checkout. Do not `cd` there, do not
`git checkout` there, do not stash there. On 2026-07-26 two sessions shared that one tree: branch
switches moved a live HEAD mid-edit and a commit landed on protected `main`. Recovery was clean only
because nothing had been pushed.

**Tick 0, before anything else:**

```bash
cd /c/Users/cdfee/projects/next-bar-share
node ~/.claude/bin/worktree-guard.mjs check     # exit 0 = safe; exit 3 = STOP, another session is here
node ~/.claude/bin/harness-state.mjs bind g-1a91cb5a-14a3-4418-8148-51cf8786e74c --json
node ~/.claude/bin/harness-heartbeat.mjs g-1a91cb5a-14a3-4418-8148-51cf8786e74c   # background
export LOOP_UNATTENDED=1
```

If `worktree-guard` exits 3, **stop and write an escalation line** — do not "just be careful".

---

## Context snapshot (the loop can execute cold from this file alone)

- Next Bar: live PWA, 406-bar catalog, Supabase auth + real follow graph, pairwise 0–10 ranking
  engine. `main` is protected (PR + `gates` CI, squash-only).
- This branch has 3 commits ahead of `origin/main`, none pushed:
  - `5e91efa` week-1 share loop (ShareButton, profile share both auth branches, `/share/[barId]`
    CTA inverted to in-product primary)
  - `d58f13f` monetization decision doc (holds the authoritative kill criterion)
  - `77ce972` CEO guard allowlist rewrite + `ceo/` folded into the main vitest include
- Gates at handoff: **669 vitest / 51 files, tsc 0**, share-card + a11y e2e 12/12 both viewports.
- `scripts/ceo-guard.mjs` is an ALLOWLIST. Adding a new orchestrator action means adding it to
  `ALLOWED` deliberately — that cost is the point, do not widen it to make a step pass.
- Analytics does NOT exist. WAU is currently unmeasurable. That gates N4.

## LOOP CONTRACT

1. Read this log; pick the next unfinished 🟢 step in order.
2. TDD where logic exists. Mirror existing patterns (`ceo/*.test.ts` for guard-style tests,
   `e2e/share-card.spec.ts` for the negative-assertion style).
3. **Verify per tick:** `npx vitest run` + `npx tsc --noEmit` + `npm run build`. Interactive steps
   also get Playwright on **both** iPhone 13 and Pixel 7 per `CLAUDE.md`.
   **BUILD FOOTGUN:** stop any dev server on :3000 and `rm -rf .next` before the first build.
4. **Review gate per substantial-T1 tick:** Reviewer A = a fresh Opus agent; Reviewer B = routed
   DeepSeek via `~/.claude/bin/harness-consult.mjs` (packets < 800 lines). Fix confirmed HIGH/MED,
   max 3 rounds, then escalate and move on. No heterogeneous reviewer available → mark the step
   NAUGHTY, log it, next step. **Never a same-model self-approval.**
5. Commit locally `[T1]`/`[T2]` with the review verdict in the message. **NEVER push.**
6. Append a tick line below. 🟢 remaining → `ScheduleWakeup(~180s)` with this same prompt.
   Else write the MORNING SUMMARY and stop.
7. **Migrations are author-only.** Write additive SQL + mock-client tests; do NOT run
   `npm run db:migrate`. `scripts/loop-guard.mjs` hard-aborts it under `LOOP_UNATTENDED=1` anyway.
8. **No Google API calls. No service-role execution. No `vercel deploy`.** Secrets stay in
   `.env.local` — never echo or commit `DATABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`,
   `GOOGLE_MAPS_API_KEY`.
9. Anything needing an operator key, account, or decision → escalation queue, skip to the next step.

## Queue

- **N1 🟢 CEO cycle runner** (goal acceptance 3). `scripts/ceo-cycle.mjs`: `measure() → assess() →
  decide() → draft() → log()` over `ceo/state.json`. Emits `ceo/reports/cycle-N.md` ≤400 words
  containing the mandatory self-audit line *"Did last cycle's recommendation produce a measurable
  change in WAU or shares? If not, why read this one?"*. Exactly ONE recommendation per cycle, with
  an expected lift and an operator-hour cost. **Must refuse to enter SHIP without a REVIEW pass** —
  test that refusal explicitly. Every action routed through `assertActionAllowed`. Metrics written
  only through `assertMeasurementUpdate`, never the agent path. Runs offline against a fixture.

- **N2 🟢 Anti-theater detectors** (goal acceptance 4). Pure module + unit tests over synthetic
  multi-cycle histories:
  - *theater tax* — `drafted > shipped` for 2 consecutive cycles → flagged in the next preamble
  - *flat-metric halt* — primary metric flat or negative for 3 consecutive cycles → emit
    `STOP_AND_REASSESS` instead of a new plan (test this path explicitly)
  - *evidence rule* — a `history` entry counts as progress only with a PR sha or a real user-event id
  - *bet closure* — a bet may not pass its `review_cycle` without a hit/miss verdict
  Wire into `decide()`.

- **N3 🟢 Dormant-module inertness.** Each dormant module (`venue_sales`, `hiring`, `finance`,
  `exit`) refuses to run and names its activation trigger. One test per module. Cheap, closes the
  "specified but inert" half of the CEO spec.

- **N4 🟡 Week-2 recipient landing** (goal acceptance 6). `/share/[barId]` renders the sender's
  ranked list above the fold and lets a **signed-out** recipient cast one 0–10 pairwise vote before
  signup — the vote IS the onboarding. e2e signed-out on both viewports, plus the negative assertion
  that casting does not force a redirect to `/auth`. **🟡 because:** persisting a signed-out vote may
  need a migration. If it does, author the SQL + mock tests, mark `PARTIAL — needs migration apply`,
  and land the client against localStorage only. Do not apply the migration.

- **N5 🔴 Week-3 analytics — DO NOT BUILD.** Three events (`share_clicked`, `share_landed`,
  `signup_from_share`) are specified, but the vendor is an open operator decision (PostHog free tier
  vs a ~20-line self-hosted endpoint) and it adds a third-party data processor, which touches
  `/privacy`. **Escalate, skip.**

- **N6 🟢 Morning summary.** Append below: shipped shas, gates at close, what's `PARTIAL`, the
  escalation queue, and an honest list of anything marked NAUGHTY.

## Escalation queue (morning)

1. **Analytics vendor decision (N5)** — PostHog free tier vs self-hosted endpoint. Blocks weeks 3–5
   of the growth plan; nothing downstream is measurable without it. `/privacy` needs updating in the
   same PR whichever way it goes.
2. **Push these branches?** `feat/share-loop-week1` (3 commits) has never been pushed. `main` is
   protected, so landing it means opening a PR. Operator call.
3. **The harness fixes live in a different repo** — `~/.claude` branch `fix/worktree-occupancy-guard`
   has 2 commits (`689a8ce` worktree guard, `ea5ec81` `harness-state move`), unpushed, 396 tests
   green. Merge to `~/.claude` master when convenient.
4. **`ceo/state.json` metrics are all `null`.** The kill criterion (50 WAU in one neighborhood /
   15 self-maintaining venues by 2026-12-31) cannot be evaluated until N5 unblocks. The clock is
   running regardless — that is deliberate.

## Ticks

### Tick 1 — N1 CEO cycle runner ✅ `1d49380`

`worktree-guard check` → SAFE. Goal bound (revision 4). `scripts/ceo-cycle.mjs` + 51 unit tests.

Verified in real Node processes, not mocks: all four SHIP refusal paths (no review, verdict
`fail`, unattributed pass, non-independent reviewer) exit 1 with the post-guard line unreached;
the legitimate path returns `shipped: true`; a clean review targeting `main` still aborts via
ceo-guard. A self-inflating measurement (`source: agent_estimate`, `wau: 999999`) is refused.

Run against the real state the runner picks `share-funnel-analytics` on its own — it reaches the
same conclusion N5 escalated, from the anti-theater eligibility rule rather than from being told.

**Review: GLM-5.2 routed (heterogeneous). 6 findings, all verified against source, all fixed** —
F1 CRITICAL measurement provenance (the guard checks which fields change, never where the numbers
came from) · F2 HIGH free-but-harmful candidate always won · F3 MED kill criterion could never
trip while the metric was null · F4 MED any non-empty reviewer string passed · F5 MED `drafted`
hardcoded · F6 LOW locale-sensitive tiebreak.

**NAUGHTY — single reviewer, not dual.** DeepSeek timed out (exit 124: unavailable, never
agreement). Reviewer A (fresh Opus agent) was not run: this session is instructed not to spawn
subagents. One heterogeneous reviewer cleared the no-self-approval bar; the second slot is unfilled
and this step has NOT had the dual review the contract asks for.

Gates: **720 vitest / 52 files** (from 669/51), tsc 0, `next build` 0.

### Tick 2 — N2 anti-theater detectors ✅ `4c2dec0`

`scripts/ceo-detectors.mjs` — pure module, 55 tests over synthetic multi-cycle histories, plus 6
wiring tests proving `decide()` actually obeys them (the part that rots silently).

All four detectors per spec. The sharp edge, stated plainly: **an unreadable metric counts as
not-improved.** If null meant "unknown, carry on", the cheapest way to never halt would be to stop
measuring — the exact failure this thing exists to notice.

Verified in a real process: 4 cycles at `wau=12` → `# CEO cycle 5 — STOP_AND_REASSESS`, deltas
0,0,0, no recommendation. Self-audit answered *"No. Cycle 4 shipped share-cta-copy and wau is
unchanged at 12"* rather than filling the section.

**Review: DeepSeek-V4-Pro routed (available again this tick). 10 findings, each checked against
source rather than taken at face value — 3 real, 3 rejected with reasons, 4 no-defect.**
Fixed: C1-part (a halted *record* could still be reviewed and shipped) · C2 (theater tax required
`drafted===true`, so producing NOTHING cleared the tax — cheapest evasion was to do less) · H2
(deltas didn't require consecutive cycles, so skipping a log entry turned a two-cycle drift into
one apparent gain). Rejected: C1's "draft() doesn't gate" is false (branches at ceo-cycle.mjs:434)
· M3 "preamble dropped from halt reports" is false (`draftHalt` renders it — now locked by a test)
· M2 is contained by the schema's required-keys check.

Gates: **781 vitest / 53 files**, tsc 0, `next build` 0.
