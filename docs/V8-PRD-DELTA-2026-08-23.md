# Next Bar V8 PRD — delta v8.4

Date: 2026-08-23
Release: `V8`
Supersedes: delta v8.1, v8.2 and v8.3 of the same date — contract version 3.1.0
Status: **FOUNDER-APPROVED AND FROZEN — 2026-08-23.** Approved by the founder together with
`docs/V8-DECISIONS-2026-08-23.md` and `docs/V8-TRACEABILITY-LEDGER.json`; see
`last_owner_approval` in the ledger for the approved contract identity.

Approval freezes the product contract. It is **not** authorization to implement: it does not
authorize goal mutation, implementation planning, product code, migrations, integration, staging,
push, PR, deployment, publication, TestFlight, or reviews.

## What this file is

A **versioned delta** against `docs/V8-PRD-2026-08-13.md`, which now carries its own dated
**Amendment — 2026-08-23 (founder approval)** section naming each superseded passage. Read both:
the base PRD still governs every requirement this delta does not name.

Requirement rows — with actor, entry point, behavior, states, audience, trust boundary, data
owner, retention, failure/recovery, accessibility, exclusions, status, owner citation, goal IDs,
acceptance references, implementation paths, candidate evidence and coverage — live in
`docs/V8-TRACEABILITY-LEDGER.json`.

**No requirement count appears in this file.** Counts are computed from the ledger by
`scripts/check-release-contract.mjs`, so a stale number cannot survive here.

**There are no unresolved product decisions.** The eight pending questions carried by v8.2 were
answered by the founder on 2026-08-23 and became `D-C-29` … `D-C-36`.

## The contract is self-contained

Every path the contract binds lives on this branch, and the validator runs with **no external
artifact root**. A run that needs `--artifact-root` to go green is **not** integration-ready and
must not be reported as such.

| Part | Path |
|---|---|
| Authoritative PRD, as amended 2026-08-23 | `docs/V8-PRD-2026-08-13.md` |
| This delta | `docs/V8-PRD-DELTA-2026-08-23.md` |
| Decision record | `docs/V8-DECISIONS-2026-08-23.md` |
| Machine-readable ledger | `docs/V8-TRACEABILITY-LEDGER.json` |
| Approval record for the design package | `docs/design-reference/README.md` |
| Approved canvases | `docs/design-reference/approved/` — **exactly 16** |
| Validator | `scripts/check-release-contract.mjs` |

---

## 1. Requirement groups

Every action drawn on an approved canvas carries its own action-level row (`kind: "action"`);
policy and lifecycle rules carry `kind: "policy"`.

| Group | Surface |
|---|---|
| `NAV` | Five-tab shell, Social sub-tabs, Account root vs Settings stack |
| `NXT` | Next Bar? five-bar home |
| `MAP` | Map exploration, markers, unified search, live filters |
| `RNK` | Rankings numeric scores and custom lists |
| `LBX` | The one shared bar lightbox |
| `SOC` | Social shell, Tonight, Plans |
| `PRE` | Manual presence and bar-level pins |
| `STO` | Stories: entry, compose, audience, lifetime, viewer, queue, tags, media privacy |
| `CAM` | Capture, review-or-retake, paired composition, review-and-tag |
| `CMP` | Global composer, four destinations, captions, receipts, deletion |
| `FEED` | Social · Feed, its audience, comments and moderation |
| `GRP` | Persistent named group chat, administration, moderation, notification volume |
| `NO` | Night Out creation, ownership, voting, media window, Saved Nights Out |
| `INV` | Invitation and token-scoped recipient |
| `ACC` | Account profile, Saved Nights Out, settings, deletion |
| `ONB` | Onboarding sequence and its auth exclusion |
| `OPS` | Operational and error states |

## 2. The 2026-08-23 founder decisions

| # | Change | Decision |
|---|---|---|
| 1 | **Feed is never public.** Its audience is all mutual friends, a named mutual-friend group, or a custom mutual-friend subset. | `D-C-29` |
| 2 | **Feed comments and moderation.** Anyone authorized to view may comment; a commenter deletes their own comment; the post author removes comments from their post; blocking stops visibility and interaction both ways; reporting hides for the reporter immediately and creates a **server-owned** operator-review record; **no new moderation dashboard** in V8; report records persist until operator removal or account deletion; deleting the post removes its comments. | `D-C-30` |
| 3 | **Persistent group chat.** Creator is the initial administrator; administrators rename and add or remove mutual friends; any member may leave; members send text and photos; any member may reuse the current membership to invite the group to a Night Out; senders delete their own messages for everyone; administrators remove messages; messages persist until removed or the group is deleted; blocking prevents new direct interaction, invitations, or addition to new shared groups; reporting as above. **In-app unread state and Night Out invitation notifications — no push per ordinary Group message.** | `D-C-31` |
| 4 | **Captions.** Global composer: optional, **max 140 characters**. Quick Add-to-Story: **caption-free**, deliberately. A Story published through the global composer may carry its caption. | `D-C-32` |
| 5 | **Multi-destination deletion.** One media object, several destination references. *Remove from this destination* removes one; *Delete everywhere* removes all; removing one never destroys the others; **bytes go only when no destination and no Saved Nights Out archive references them**; removing Feed also removes that post's comments. | `D-C-33` |
| 6 | **Success receipts.** Feed only "Posted to Feed" (View post, Undo); Story only "Added to your story" (View story, Undo); multiple "Shared to N places" with indicators (Done, Undo). | `D-C-34` |
| 7 | **Ranking events.** No automatic Feed entries in V8; deferred to V9. | `D-C-35` |
| 8 | **Presence and pin.** No automatic GPS/location tracking; presence is manual; Going out / Maybe later / Not going out; Pin or Heading-to is an explicit action that **sets Going out** and the bar until 4:00 AM; Maybe later or Not going out **clears** the pin; both expire at 4:00 AM; **RSVP never silently controls presence**. Replaces "overrides automatic presence". | `D-C-36` |
| 9 | **Photos library.** Native Photos-library export or save is **entirely deferred to V9**, unconditionally. Saved Nights Out stays in V8 as a private **in-app** archive and is not the Photos library. | `D-C-19` (revised) |

## 2b. Contract revision 3.1.0 — three new bindings and two corrections

Approved by the founder on 2026-08-23. `PB-01`–`PB-03` **add authoritative meaning**, so this is a
minor contract revision (`3.1.0`), not a correction-only `3.0.1`.

| # | Change | Decision |
|---|---|---|
| 10 | **Named-group audience is intersected.** For **Story**, **Feed** and **pin** audiences, recipients equal the selected group **intersected with the poster's or pinner's own mutual friends**. The intersection is computed and enforced **server-side**; an unresolvable mutual-friend set **fails closed** rather than delivering to the unintersected group. **The persistent Group-chat destination is the deliberate exception** — it retains its **fixed** Group membership, because the post lands in the group thread itself rather than in a derived audience. | `D-C-37` |
| 11 | **One Group administrator**, initially the creator. On the administrator **leaving or deleting their account**, administration transfers to the **longest-standing remaining member**, tie-broken deterministically by **lowest member profile id**. With **no member remaining the Group is deleted**. | `D-C-38` |
| 12 | **The 4:00 AM boundary uses America/New_York** and the existing **DST-aware night-key convention**. A UTC comparison is a defect. | `D-C-39` |

**Corrections (no settled meaning changed).** `CC-01` repairs `V8-R-STO-016.behavior` and `CC-02`
repairs `V8-R-CMP-003.failure_recovery`, both of which described the **already-settled** `D-C-33`
deletion rule as still open. The rule itself is unchanged: removing one destination does not destroy
the remaining destinations, and bytes are reclaimed only when no destination and no Saved Nights Out
archive references them.

**Rows updated by this revision (14):** `V8-R-STO-009`, `V8-R-STO-016`, `V8-R-FEED-006`,
`V8-R-CMP-003`, `V8-R-CMP-005`, `V8-R-CMP-007`, `V8-R-GRP-001`, `V8-R-GRP-005`, `V8-R-GRP-006`,
`V8-R-GRP-009`, `V8-R-PRE-001`, `V8-R-PRE-002`, `V8-R-PRE-004`, `V8-R-PRE-005`. No status, coverage,
evidence, implementation path, goal mapping or approved-artifact identity changed anywhere, and the
16 canvas digests are untouched.

## 3. Group notification volume — the prerequisite the base PRD required

The base PRD made moderation, blocking, retention, abuse **and notification-volume** policy a
precondition of considering group chat. `D-C-31` supplies all of it. The notification-volume half
is recorded explicitly because it was the piece previously omitted:

- V8 provides **in-app unread state** for Group threads.
- V8 sends **Night Out invitation notifications**.
- V8 **does not send a push notification for every ordinary Group message**.
- The `Group activity` notification category defaults to **Mentions only**.

Carried by `V8-R-GRP-008` (policy) and reflected in `V8-R-ACC-010`.

## 4. Superseded PRD text

Each is also marked in the PRD's own amendment section.

| Superseded | By |
|---|---|
| "Group chat is excluded from V8; moderation, blocking, retention, abuse, and notification-volume policy must exist before chat is considered." | `D-C-20` for the exclusion; `D-C-31` **supplies** the policy set. Not waived. |
| "`Not tonight` is the user-facing declined action." | `D-C-22`, with presence keeping Not going out per `D-C-36` |
| "Pre-signup RSVP … is not authorized by this PRD." | `D-C-23`, for RSVP only |
| "Joining, accepting, declining, suggesting, and voting require authentication." | `D-C-23`, narrowed to joining, suggesting and voting |
| Ratings ownership without a named conflict rule | `D-C-26` |
| `next-bar-rankings-numeric-lists` footer "Your scores · stored on this device" | `D-C-26` |
| `next-bar-map-v1` draft filter state and its Apply / "Show N bars" commit gate; the six-axis Tweak-the-vibe Map surface | `D-C-09`, reaffirmed by `D-C-25` — recorded as **`V8-R-MAP-007`** |
| In-canvas "EXPLORATORY — REVIEW NEEDED" / "ASSUMPTION — PENDING APPROVAL" banners | `D-C-27` |

## 5. The Map, stated precisely

The current Map candidate is goal `g-17984023-b73c-4a59-a9b2-030d9909a21c` at
`aa22a660dfdfe10e634a043450326109c4dd45e5`. These approved behaviors are **preserved and current**:

- one **unified search** over neighborhoods, neighborhood acronyms, vibes and individual bars
  (`V8-R-MAP-003`);
- a **compact filter sheet** offering neighborhood, distance and vibe options, applying **live**,
  with **removable filter chips**, and **no nested Apply or "Show N bars" step**
  (`V8-R-MAP-004`, **approved**);
- the **shared BarLightbox** (`V8-R-MAP-006`, `V8-R-LBX-001`).

The obsolete behavior is recorded separately in **`V8-R-MAP-007`** (**superseded**): draft filter
state, the Apply / "Show N bars" commit gate, and the six-axis Tweak-the-vibe Map surface. Goal
`g-12d33864-35a6-4472-9f13-04d352a64869` is **historical and stale** — not to be unblocked,
modified, merged, or treated as current product authority. It is attached to that superseded row
only.

**The Map is not wholly current.** `V8-R-MAP-002` — marker derivation from the numeric score — is
**contradicted** on the shipped candidate, which still reads the legacy Loved/Liked/Pass tiers.
That is identified future implementation work, not a resolved item.

## 6. Deferred to V9

| Deferred | Decision |
|---|---|
| Badges | `D-C-24` |
| Persona card | `D-C-24` |
| Automatic ranking-event Feed rows | `D-C-35` |
| Native Photos-library export or save — **unconditional** | `D-C-19` (revised) |

## 7. Explicit exclusions

- **Third-party authentication** (Apple, Google, phone) — `D-C-28`, with the two dependent
  operational-state cards.
- **Gender field** in onboarding — `D-C-04`.
- **A public Feed audience** — `D-C-29`. **A public Story audience** — `D-C-17`.
- **Delivering a Story, Feed post or pin to a group member who is not a mutual friend of the actor** — `D-C-37`. The Group-chat destination is the recorded exception.
- **A Group with no administrator**, and **a UTC 4:00 AM comparison** — `D-C-38`, `D-C-39`.
- **A public comment thread on a Story** — `D-C-16`.
- **Public like counts and follower metrics** on Feed and Stories.
- **Automatic publication of a Story to Feed**, and **duplicated media across destinations** —
  `D-C-14`.
- **A push notification per ordinary Group message** — `D-C-31`.
- **Any automatic GPS or location tracking**, and any RSVP-driven change to presence — `D-C-36`.
- **Native Photos-library export** — `D-C-19`.
- **Hide / "never show me this again"**, and any rating control on Next Bar? — base PRD P1.
- **Loved / Liked / Pass tiers and a suggested-tier marker** — `D-C-11`, `D-C-25`.

## 8. Approved requirements with missing implementation

`V8-R-STO-014` (server-side re-encode and EXIF/GPS stripping) and `V8-R-STO-015` (server-enforced
signed-URL lifetime) are **approved** security requirements whose implementation does not exist
yet. They read `status: approved`, `coverage: missing`. Missing implementation is **not** an
unresolved decision and must never be recorded as `blocked`.

The validator enforces the distinction: **a `blocked` row must cite a currently open
product-decision id**, and there are none.

## 9. Evidence owed, distinct from behavior settled

Three live suites have **never executed** — never passed, never failed, never forced green:
`storiesRls.live.test.ts`, `friendScoreRls.live.test.ts`, `nightOutsRls.live.test.ts`. Every
RLS/authorization claim on the Stories rows is therefore **asserted, not proven**. Approval of
this delta is not verification.

## 10. Change control

Approval freezes, together and by digest: this delta, the decision record, the ledger, the
authoritative PRD, the design-reference README and all 16 approved canvases. The ledger binds the
first five by SHA-256; the validator emits the ledger's own digest on every run, and that value is
recorded with the approval.

```
node scripts/check-release-contract.mjs docs/V8-TRACEABILITY-LEDGER.json
CI=1 npx vitest run scripts/check-release-contract.test.ts
```
