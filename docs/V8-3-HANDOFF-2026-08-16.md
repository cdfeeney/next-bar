# V8-3 handoff — 2026-08-16 (operator-directed land; old-harness session closed)

**Goal:** `g-6ec6673a-2ecd-4b17-9a15-74641ee859d4` — V8-3 Canonical Night Out
invitation lifecycle. **Resume with `/code g-6ec6673a…` in a fresh session —
never recreate the goal.** Store state at land: `in_progress` /
`implementation`, mid review-cycle-1 fix round (`begin-fix` already run).
This file exists because the store rejects `record-evidence` during the
implementation phase; everything below is the context that couldn't land in
the goal.

## Where the code is

Worktree `D:/harness-worktrees/nb-v8-max-parallel-20260814/core-social`,
branch `harness/v8-max-parallel-20260814/core-social`, HEAD `117c2b1`, clean.
Base for the candidate: `83060acbcbc6c74f7fa103825bd6d6df5297535a` (full sha —
the store rejects short shas).

- `b4512fe` — the full V8-3 feature. **This commit was fully verified** at
  candidate `2000f81a…`: 1146 vitest, typecheck, build, 42/42 release e2e,
  zero retries (evidence recorded in the store on that candidate).
- `7627b42` + `117c2b1` — all **nine** round-1 panel findings fixed in code.
  **Gates NOT re-run on these.** Resume step 1 is the full gate.

## Round-1 panel (receipts recorded on candidate `2000f81a…`)

Codex (6): no create entry point → `StartNightOutButton` on the consensus
page; link-visit silently re-accepted a declined member → `join_night_out_by_token`
now accepts only from `pending`; duplicate lifecycle events on conflicted
inserts → `IF FOUND` gates; behavioral RLS negatives impossible unapplied →
attended APPLY GATE checklist added to the migration header; cancelled plans
writable via vote/respond → status gates added; onboarding could consume-and-
lose the invite context → redirect peeks, destination page consumes.

Claude (3): page called join just to VIEW → new `resolve_night_out_by_token`
member-scoped definer READ + explicit **Join** / **I'm in** buttons (viewing
never mutates); `create_night_out` uncapped → cap 10 live plans + advisory
lock; `PendingInviteRedirect` skipped consuming on any `/night-out/*` → now
skips only the pending token's own path.

## Resume sequence

1. Full gate: `vitest` + `typecheck` + `build`; then
   `PLAYWRIGHT_PORT=3612 PLAYWRIGHT_RELEASE=1 npx playwright test --retries=0 --workers=1`
   in batches (`night-out`+`friends-flow` / `v7-continuity`+`rating-and-nav`+`account-delete`
   / `pairwise-flow`+`rankings-score`). Serialize (`--workers=1`) — parallel
   runs OOM'd this laptop repeatedly.
2. Fix any red; then `phase verification` → `record-evidence` →
   `candidate --base 83060acbcbc6c74f7fa103825bd6d6df5297535a --intensity codex --tier T0`.
3. Fresh two-lane panel. Claude gating reviewer: its prompt MUST embed
   `candidate_id` + `packet_sha256` (the receipt sealer verifies this) and
   demand ONE fenced ```json block, lowercase severities, keys
   `verdict/findings/severity/file/summary/trigger` only (the store rejects
   anything else as malformed). Codex lane: `bin/codex-review.mjs` — runs
   UNSANDBOXED on this machine (read-only sandbox broken:
   `CreateProcessWithLogonW failed: 2`, suspected Win11 Home; operator-approved
   fallback documented in the adapter).
4. Reproduce-or-adjudicate findings (`finding-digests.mjs` → adjudication
   JSON → `record-adjudication`), then `finalize-reviewed` + release.

## Tier and residuals

- **T0** via the project tier-map (`supabase/migrations/**`). Migration
  `0021_night_outs.sql` is committed **UNAPPLIED** by spec; the attended apply
  carries the T0 gates and the behavioral RLS probes (checklist in the
  migration header). Numbering note: spec reserved 0020; V8-2 shipped 0020
  first (note in-file).
- V8-2 (complete) also shipped migration `0020_pairwise_tuple_unique.sql`
  reviewed as T1 before the tier-map existed — its attended apply should get
  the same T0 treatment.

## Session-level protocol lessons (cost real time twice)

- **Never edit or commit while a candidate panel is open** — `begin-fix`
  first. Recovery when violated: commit to a tmp branch, `git reset --hard`
  to the candidate sha, complete the panel, `begin-fix`, `merge --ff-only`.
- **Inline lease heartbeat before every store mutation**; background
  heartbeats silently die and re-acquire with a new token
  (memory: `lease-heartbeat-must-be-inline`).
- **Redirect `bind` output to a file before parsing** — the lease token is
  printed exactly once (memory: `bind-returns-the-lease-token-once`).

## Board at land

V8-2 ✅ complete/finalized · V8-5 ✅ · **V8-3 this handoff** · V8-4 blocked
(unblocks when V8-3 finalizes; same store, same recovery: `lease acquire` +
`set-status in_progress --expect <revision>`) · V8-1 blocked in
`nb-v8-integration-20260813` (host-degradation root cause at `4c1a35e`; needs a
serialized re-verify, its stale lease is reclaimable) · worktree cleanup
inventoried but deferred (dirty: `nb-overnight` 8 files, `nb-qa4` 18 files —
inspect before removing; clean+merged: `nb-v7-integration`) ·
`D:\ClaudeData\.claude\commands\code.md` contains an unresolved `<<<<<<< HEAD`
conflict marker from the v3-harness migration session.
