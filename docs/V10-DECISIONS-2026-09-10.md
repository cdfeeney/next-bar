# Next Bar V10 — decision record

Date: 2026-09-10 (decisions taken by the owner on 2026-09-09 while checking TestFlight build 10 / the staging
website). Status: **owner decisions, recorded by the V10 overnight run.** Recording is not authorization to
release; the owner pushes and deploys.

| ID | Decision | Effect |
|---|---|---|
| D-V10-01 | **V8-R-SOC-003 (the "Next Bar?" card on Social › Tonight) is retired.** Owner, 2026-09-09: "on Social why do we have a Next Bar row there — please delete that, it doesn't make sense." | `src/app/friends/page.tsx` no longer renders the card; its e2e case in `e2e/night-out.spec.ts` is deleted. Tonight is the stories rail, presence and Groups & people. The V8 traceability ledger row stays as history (the ledger is digest-frozen); this record supersedes its `approved` status for SOC-003. |
| D-V10-02 | Small uppercase labels (bottom nav, section labels, chips, ≤12 px uppercase buttons) use the body face, Nunito Sans 600 (`font-label`). Playfair Display stays on headings, eyebrows and ≥14 px buttons. **Face superseded by D-V10-04; the size rule (≤12 px uppercase = label role) stands.** | See `docs/V9-TYPOGRAPHY-CONTRACT-2026-09-09.md` (label role) and V10-01. |
| D-V10-03 | **V10-06 (owner, 2026-09-09, build 11): the bottom nav goes back to the V8 face, Poppins Bold, with the active highlight sized to the label.** Commit c2162cd. | Superseded for the nav by D-V10-05 the same week; recorded because TestFlight build 12 shipped it. |
| D-V10-04 | **V10-07 (owner, 2026-09-11, on staging): the whole app is back on the V8 Poppins kit.** "Like V8" = restore. One face, Poppins 400/500/600/700; display 700, label 700, body 400. Playfair Display and Nunito Sans are removed. Commit 5847c15. | `src/app/layout.tsx`, `tailwind.config.ts` (all four tokens → Poppins), `src/app/globals.css` weights, `e2e/typography-contract.spec.ts`. Supersedes the face named in D-V10-02 and the V9-11 pair. |
| D-V10-05 | **V10-08 (2026-09-11): the bottom nav is the iOS HIG tab bar** — 49pt + safe area, glyph over a 10px sentence-case label, tint-only active state (`aria-current`), no pill, no uppercase; `font-nav` weight 600 so "Next Bar?" fits at 320px. ACCOUNT no longer runs off the edge. Commit 522189c. Reviewed 2026-09-14 (Fable + Codex, APPROVE-WITH-FINDINGS; findings were doc drift, fixed by R-01). | `src/components/BottomNav.tsx`, `src/app/globals.css` (`.font-nav { font-weight: 600 }`), `e2e/mobile-controls.spec.ts`, `e2e/typography-contract.spec.ts`, `e2e/home-location-first.spec.ts`. |
