# Night Log — 2026-07-25 (overnight loop, drafted 2026-07-24 ~21:00 ET)

Continues `BLUEPRINT-beta-enterprise-2026-07-23.md` under goal `g-f04c56d0`. One 🟢 step per tick. Keep build green. Escalate-don't-fake. **Never push.**

## Context snapshot (loop can execute cold from this + blueprint)
Done + committed local: B0 gate, B1 catalog, B2 usernames (LIVE), B3 follows (LIVE), B4 comparison chains, D3 Places (LIVE: hours/open-now/closed-filter), demo circle = Claire+John. Migrations 0000–0007 applied to live Supabase. B5 (photo cards) + B6 (tiered map) landing at loop start — if their commits exist, skip to queue; else finish their integration FIRST (reviews already run; see git status).
Gates at draft time: 433 vitest, tsc 0, build clean, 153+ e2e.

## LOOP CONTRACT
1. Read this log + blueprint; pick next unfinished 🟢 step.
2. TDD where logic exists. Mirror existing patterns (dual-mode hooks, 0006/0007 SQL grant patterns, accountCache rules, pressSequentially e2e helper).
3. Verify per tick: `npx vitest run` + `npx tsc --noEmit` + `npm run build`. **BUILD FOOTGUN:** a dev server may hold port 3000/.next — TaskStop it + `rm -rf .next` before the first build; leave it stopped overnight.
4. Review gate per substantial-T1 tick: `/santa-loop --unattended` semantics — Reviewer A fresh Opus agent + Reviewer B routed DeepSeek via `harness-consult.mjs` (small packets <800 lines; timeouts on big ones). No heterogeneous reviewer available → mark step NAUGHTY, log, move to next step (fail-closed, never same-model-pair approval). Fix confirmed HIGH/MED, max 3 rounds, then escalate + next step.
5. **MIGRATIONS ARE AUTHOR-ONLY OVERNIGHT (DeepSeek security review 2026-07-24):** write + commit the additive SQL and its code, write tests against a MOCKED client, but **DO NOT run `npm run db:migrate`** — the runner hard-refuses under `LOOP_UNATTENDED=1` anyway (service-role/pooler creds must not touch the live DB unsupervised). Each migration goes to the morning escalation queue for attended apply. A step whose behavior can't be verified without the applied migration: land the code + mock-tests, mark the step `PARTIAL — needs migration apply`, continue.
6. Commit locally `[T1]`/`[T2]` with review verdict in message; NEVER push; append tick line below.
7. 🟢 remaining → ScheduleWakeup(same loop prompt, ~180s). Else morning summary + stop.
8. **Google API rule (now technically enforced):** the loop runs with `LOOP_UNATTENDED=1`; `refresh-places.mjs`, `ingest-bars.ts`, and `apply-migrations.ts` all HARD-ABORT under it (scripts/loop-guard.mjs). Ingest already done by lead pre-loop. Never unset the flag.
9. Secrets stay in `.env.local`. Never echo/commit `DATABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, or `GOOGLE_MAPS_API_KEY`. Anything needing operator account/key/decision → escalation queue.
10. **No service-role scripts overnight:** account-deletion (N3) uses the service-role key — author the route + mock-tests only; do NOT execute it against live auth. Same author-only rule as migrations.

## Queue (order)
- **N1 🟢 B3b follow requests + privacy toggle** (blueprint §B3b — full spec there). Migration 0008; follow_user behavior change for private targets; accept/decline RPCs + inbox on /friends; Settings is_private toggle (column grant exists); "Requested" button states; unit + e2e (stubbed).
- **N2 🟢 H1 App-Store pack**: /privacy + /terms pages (honest v1 copy, placeholders marked), 21+ age gate (localStorage-ack overlay, `next-bar:age-ack:v1`, NOT in accountCache), `/api/health` (checks Supabase reachability, returns build sha), waitlist hardening (email regex, rate limit by IP-in-memory, generic errors — audit MED-23).
- **N3 🟢 H2 account deletion**: service-role API route (pattern: api/waitlist + set-password.mjs key handling — key read server-side ONLY from env, never bundled), deletes auth user + cascades; Settings "Delete account" with type-to-confirm; signs out + clearAccountCache after.
- **N4 🟢 P1 push scaffolding dark**: SW push/notificationclick handlers, subscription client + `push_subscriptions` migration (additive, RPC-write pattern), gated by cadence.ts; ships dark (no VAPID) behind `NEXT_PUBLIC_PUSH_ENABLED` absent.
- **N5 🟢 B7a insertFlow dynamic candidates** (blueprint §B7 queued item): recompute candidates from replay after each answer; unordered peers excluded from probe set once an order exists; property tests incl. Codex's u1/u2/u3 interleave case + n≥128 approximate-placement assertion.
- **N6 🟢 B7b eval suite**: `src/lib/__evals__/` — pairwise invariants + transitivity sampling, matching monotonicity (jaccard & distance), exploration slot (1 of top-10 = qualified long-tail pick, deterministic seed), 5k perf under CI ceiling.
- **N7 🟢 B5c-1 tag vocabulary audit**: agent reads all 33 VibeTags + usage counts across catalog + matching semantics → `docs/TAG-AUDIT-2026-07-25.md` (gaps/overlaps/rename proposals — proposals ONLY, no catalog edits).
- **N8 🟢 B5c-2 identity + dead-bar pass**: from `scripts/refresh-report.json` flags — propose catalog removals for the ~30 CLOSED_PERMANENTLY bars + quarantined wrong-venue matches (bootleg-bar) → diff proposal in `docs/CATALOG-CLEANUP-2026-07-25.md`; a SECOND adversarial agent confirms each (web-verifiable name+address reasoning from catalog data only — no network). Confirmed-by-both CLOSED_PERMANENTLY removals MAY be applied to bars.*.ts (bars.test.ts guards); anything contested → operator queue.
- **N9 🟢 U1 polish batch** (audit MEDs): map pans to user fix (MED-15), quiz highlight parity (MED-11), "near you" vs neighborhood-filter honesty (MED-12), pass-tap undo snackbar (MED-14), `/` links quiz for profile-less users (MED-26), /join success CTA.

## Escalation queue (morning)
0. **APPLY migrations 0008 + 0009** via attended `npm run db:migrate` — B3b follow requests + P1 push_subscriptions (0009 is additive and dark; nothing reads it until VAPID keys land). Until applied: privacy toggle stays hidden (runtime feature-detect, fail-closed), follow_user keeps pre-0008 boolean behavior, Requests inbox renders empty. Client is committed compatible with BOTH sides of the apply. NOTE: 0008 DROPs+recreates follow_user (return type change) — apply in a quiet moment.
1. Google console: drop GetPlaceRequest 1500→100, SearchTextRequest 600→50, GetPhotoMediaRequest 600→100.
2. Brevo SMTP finish (if not done) + Twilio paste into Supabase (phone OTP UI then becomes a 🟢).
3. Push/deploy decision: 30+ reviewed local commits — say "push it" for Vercel.
4. B8 final gate needs: two real accounts walkthrough on phone (operator) + VAPID/analytics decisions.
5. Operator walkthrough note: signed-in /friends/consensus + Tonight demo sections render empty-ish (real graph has no demo picks) — known, B3 report.
6. **iPhone-13-only e2e failures grew to 3 suites** (all proven pre-existing relative to the tick that found them, via git-stash baseline runs): map single-finger pan, rating-and-nav "Pass-rated excluded", and rankings-add-flow x2 ("navigation to /rankings interrupted by navigation to /"). Pixel 7 fully green throughout. Suspicion: WebKit emulation + dev-server drift, possibly the N4 sw.js byte-change re-install. Morning: warmed re-run; if persistent, bisect N2→N4.
6b. (superseded original note kept for history) 2 pre-existing e2e failures on iPhone 13 only (proven pre-N2 via baseline stash re-run): map single-finger pan (transform never changes) + rating-and-nav "Pass-rated bars excluded" — likely emulation drift since handoff; Pixel 7 green. Worth a warmed re-run / look in the morning.
7. Legal copy: /privacy + /terms are honest v1 drafts with visible [PLACEHOLDER] markers — need operator + legal review before App Store submission.
8b. **ENGINE FINDING (N6 evals, Opus-confirmed, PRE-EXISTING):** multi-device merge re-sorts the pairwise transcript by compared_at, interleaving two sessions into the 'random-order' shape where the replay heuristic measured ~29% mean latest-row violations (session-shaped input = 0%). reconcileScores does NOT correct it (same order-sensitive replay). The session_id column already anticipates a fix (per-session replay then merge). Deferred, tracked — real-world two-monotone interleave is milder than the measured ceiling.
8. **Account-deletion go-live needs two operator decisions:** (a) set `SUPABASE_SERVICE_ROLE_KEY` in the Vercel env (route 503s 'unavailable' without it — safe default); (b) DeepSeek raised that industry practice wants FRESH re-authentication before deletion (any valid session token currently suffices; type-to-confirm is client-side only). Decide: ship as-is for beta, or add a reauth step first. Also ensure LOOP_UNATTENDED is never set in production env (route hard-503s under it, logged).

## Ticks
(appended by the loop)
- **Tick 7 — N7 🟢 B5c-1 tag vocabulary audit → DONE.** Commit 945a009. docs/TAG-AUDIT-2026-07-25.md from committed counting tool (238 open bars). Headlines: F1 hiphop dead-tag-with-live-quiz-path (0 bars); F2 11/33 tags quiz-inexpressible incl. date(84)/beer(76); F3 near-dup pairs as B5c priors; F5 price tags duplicate priceTier. Proposals only — zero catalog edits; action order proposed for operator. T2 tier → advisory review (script-derived data), no dual gate.
- **Tick 6 — N6 🟢 B7b eval suite → DONE.** Commit d2d36bd. Eval suite (src/lib/__evals__): session-shaped replay = ZERO violations (pinned); random-order = heuristic w/ dual fence (measured mean 0.293/p95 0.500 via committed calibration script); matching monotonicity, neighborhood honesty, exploration properties, 5k perf ceiling. NEW exploration slot: quiz+map (10-result surfaces) reserve last slot for a nightly-rotating long-tail pick (5am local rollover). Reviews: both PASS after fixes (DeepSeek 2 HIGH: UTC-rotation + mean-only fence, fixed; Opus M2 finding escalated below). Gates: 507 vitest / tsc 0 / build clean / quiz+map+where-next e2e 10/10 Pixel.
- **Tick 5 — N5 🟢 B7a insertFlow dynamic candidates → DONE.** Commit 6ca1970. Candidates recomputed from replay per answer; unordered peers out of the probe set (bootstrap excepted); per-pair later-wins window (mind-change ≠ conflict); exact slots vs ordered chain; n≥128 approximate-by-policy asserted (n=300); u1/u2/u3 exhaustive-path test. Reviews: Opus PASS (1 MED + 1 LOW fixed, 1 LOW noted); DeepSeek errored once → --force retry OK (H1 dup of Opus MED fixed; H2/M1 refuted w/ reasoning; M2-M4 agree/documented). Gates: 496 vitest / tsc 0 / build clean. iPhone-13 e2e failures (rankings-add-flow) proven pre-existing via stash baseline → escalation item 6 updated.
- **Tick 4 — N4 🟢 P1 push scaffolding dark → `PARTIAL — needs migration apply (0009)`; code COMPLETE + dual-review PASS, ships dark.** Commit d67171e. SW push/notificationclick (same-origin-constrained sinks), push.ts client (dual-gated dark, cadence-gated opt-in prompt, rollback on server refusal), migration 0009 (RPC-only writes, unique endpoint owner + transfer, advisory-locked 10-device cap). Reviews: Opus PASS (1 MED + 2 LOW fixed); DeepSeek 2 HIGH + 1 MED confirmed→fixed (open-redirect, endpoint collision, cap TOCTOU), 3 notes verified non-issues. Gates: 490 vitest / tsc 0 / build clean. No e2e (dark feature — nothing user-reachable).
- **Tick 3 — N3 🟢 H2 account deletion → `PARTIAL — needs operator go-live (service key in deploy env + reauth decision)`; code COMPLETE, dual-review PASS, author-only rule held (zero live-auth execution).** Commit d6e2918. Route: LOOP_UNATTENDED hard-503 (logged) → 5/hr IP limit → env check → bearer → getUser verify → deleteUser(verified id only); generic errors; transport throws caught. Settings danger zone w/ type-to-confirm, honest failure, try/finally redirect, a11y focus. Tests: 12 mocked route/helper units incl. adversarial-body + throw cases; 6 e2e vs browser-boundary stub. Reviews: Opus PASS (2 MED fixed, 2 LOW fixed, 2 LOW accepted); DeepSeek HIGH-1 token-confusion refuted w/ documented reasoning, HIGH-2 fresh-reauth → escalation item 8, MED gate-logging fixed, MED cascade-triggers verified n/a. Gates: 478 vitest / tsc 0 / build clean / 6 of 6 e2e.
- **Tick 2 — N2 🟢 H1 App-Store pack → DONE.** Commit 778db85. /privacy + /terms (honest v1, [PLACEHOLDER]-marked), 21+ age gate (device-level ack key, NOT in accountCache; z-2000 after e2e caught nav clickable under z-100), /api/health (30s-cached probe, 12-char sha, 503 unreachable), waitlist MED-23 hardening (validation, gmail dot-collapse, 10/hr/IP limiter w/ fail-closed 10k cap, generic errors, 23505=success anti-oracle). E2e: global storageState pre-ack + 3 clear-site re-seeds + new 10-test spec. Reviews: Opus PASS (privacy-copy vibe_profile honesty MED → fixed), DeepSeek 5 MED (4 fixed, 1 accepted w/ rationale). Gates: 467 vitest / tsc 0 / build clean / 10 of 10 spec e2e / full suite 174 pass + 2 iPhone-13 failures proven pre-existing (escalation item 6).
- **Tick 1 — N1 🟢 B3b follow requests + privacy toggle → `PARTIAL — needs migration apply` (code COMPLETE + dual-review PASS).** Commit 5d25633 (+ pre-loop leftovers committed as 7fefe7f). Migration 0008 authored UNAPPLIED (author-only rule): follow_requests table (RPC-only writes, parties-read RLS), follow_user boolean→text ('followed'/'requested'/'rejected', shared 100/day cap), accept/decline/cancel RPCs, get_follow_requests + get_outgoing_requests definer reads. Client: follows.server wrappers (dual-generation return mapping), useFollows requested[] state + withdraw, new useFollowRequests inbox hook, /friends Requests section + 3-state buttons, Settings privacy toggle gated by runtime 0008 feature-detect (fail-closed). Reviews: Opus r1 FAIL (HIGH: pre-0008 consent promise; MED: double-tap phantom, missing epoch guards; 2 LOW) → all fixed → r2 PASS. DeepSeek: accept-RPC half-state confirmed→fixed (single savepoint); is_private-NULL refuted (NOT NULL since 0001); privacy oracle accepted-by-design, documented in SQL. Gates: 450 vitest / tsc 0 / prod build clean / 10 of 10 new stubbed e2e (both devices). Dev server on :3000 stopped per contract (stays stopped). Escalation queue gained item 0 (apply 0008).
