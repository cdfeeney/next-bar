# Night Log — 2026-07-24 (overnight queue, drafted 2026-07-23 evening)

Executes the fix + build queue from `UI-PATH-REVIEW-2026-07-23.md` (finding numbers cited below) and `CAPABILITIES-REVIEW-2026-07-23.md`. One 🟢 step per tick. Keep build green. Escalate-don't-fake. **Never push.**

## LOOP CONTRACT (self-contained — a fresh session can execute cold from this file + the two review docs)

**Order (revised 2026-07-23 late — supersedes original):** F1 → F2 → F3 → F4 → F5 → F6 → F7 → **B0.2 → B0.3 → B0.4** (specs in `BLUEPRINT-beta-enterprise-2026-07-23.md`; they REPLACE this file's old S1 — pairwise server sync must not ship on the unfixed algorithm) → H1 → H2 → P1 → U1. **Phase D1 (D1a–D1d) below is SUPERSEDED by blueprint steps B2/B3 — do NOT execute it**: B3's migration exposes tier only (never score) to friends and needs the operator's `is_private` decision. F-steps are independent bug fixes (safe in listed order).

**Per tick (exactly ONE step):**
1. Read this log + the referenced finding; pick next unfinished 🟢 step.
2. TDD: failing test FIRST for every bug fix (vitest for lib/hooks; Playwright where the bug is flow-level). Mirror existing patterns (`useRatings` dual-mode, `pressSequentially` for WebKit inputs, aria-scoped locators).
3. Verify ALL: `npm test && npx tsc --noEmit && npm run build`. Never build while a dev server holds port 3000/.next — TaskStop + `rm -rf .next` first.
4. Red and unfixable this tick → revert so the tree stays green.
5. Commit locally `[T1]`/`[T2]` with finding refs; NEVER push; one worktree.
6. Append tick line below (step · status · sha, or ESCALATED + why).
7. 🟢 steps remain → ScheduleWakeup(same loop prompt, ~120s); else morning summary + stop.

**DB rule for D1a:** additive-only migrations (new tables/policies; never ALTER/DROP existing). Apply with the existing `scripts/apply-migrations.ts` flow; record the migration filename as the revert point. If apply fails → commit SQL only, ESCALATE the apply.

**Hard rules:** service-role key stays in `.env.local`, never in code/docs. Anything needing an operator account/key/decision → escalation queue, skip to next 🟢.

## Queue

### Phase F — stop the sign-in downgrade + top HIGHs (all 🟢)
- **F1** Server-mode rating broadcast: dispatch + listen `SERVER_BROADCAST` in `useRatings.ts` so signed-in taps update ResultsView/map/badges live. (HIGH-1)
- **F2** "Clear all ratings" in server mode deletes server rows (await, then reload) AND removes `next-bar:pairwise:v1`. (HIGH-2, MED-24)
- **F3** Demo pollution guard: exclude sample-night ratings from the sign-in merge (`useRatings.ts:108-111`), add visible "demo" tags on demo friends/intents, and stop static every-night intents implying real activity. PLUS cleanup migration **0003**: delete already-merged sample-night rows from real accounts (demo bar ids enumerable from `src/lib/demo/`). (HIGH-4, HIGH-7; blueprint B0.1)
- **F4** `/auth` reads `?error=` searchParams → friendly expired/reused-link messaging with resend path. (HIGH-5)
- **F5** Intent 5am rollover: `useIntent` re-evaluates expiry (visibility/interval), tap-to-clear clears the *displayed* state; recompute cadence banner past midnight. (HIGH-6, LOW-cadence)
- **F6** Delete-list confirm; share-cancel must not write clipboard on `AbortError`; `/share/[barId]` CTA targets the bar (map/where-next seeded), not the marketing homepage. (HIGH-8, MED-16, MED-17)
- **F7** GpsConfirm `requesting` branch — spinner instead of "can't confirm"/auto-proceed while fix is in flight. (HIGH-10)

### Phase B0 — algorithm + sync correctness (🟢; REPLACES old S1 — full specs in `BLUEPRINT-beta-enterprise-2026-07-23.md` §B0.2–B0.4)
- **B0.2** Pairwise rank-order model (ordered list per tier, score = band interpolation by index; fixes pairwise.ts:205 + :230-233 by construction; `comparisonCount`; property tests).
- **B0.3** Sync hardening: migration 0004 — `ratings.updated_at` + LWW BEFORE-UPDATE trigger, post-merge hydrate (incl. score), `session_id` on pairwise_comparisons.
- **B0.4** Pairwise server sync + score sync + re-enable signed-in prompts. Exact `ratings.server.ts` changes listed in the blueprint (select/preserve `score`, `updated_at`).

### ~~Phase D1~~ — SUPERSEDED (2026-07-23) by blueprint B2 (usernames) + B3 (friends). Do not execute: B3 ships follows + friend-read RLS in ONE migration (0006) exposing tier only — never score — and awaits the operator's `is_private` default decision.

### Phase H — App-Store hardening (🟢)
- **H1** `/privacy` + `/terms` pages, 21+ age gate, `/api/health` endpoint. Also: validate email + rate-limit + generic errors on `/api/waitlist`. (MED-23)
- **H2** Account-deletion endpoint (service-role, mirrors waitlist route pattern) + Settings "Delete account" with confirm.

### Phase P — push scaffolding dark (🟢)
- **P1** SW `push`/`notificationclick` handlers, subscription client + table (additive migration), gated by `cadence.ts`; ships dark until VAPID/send infra.

### Phase U — polish batch (🟢, single tick, judgment picks)
- **U1** Highest-value MEDIUMs: map pans to user (MED-15); quiz highlight parity (MED-11); "near you" vs neighborhood-filter labeling (MED-12); pass-tap undo snackbar (MED-14); `/` links to quiz for profile-less users (MED-26); `/join` success CTA.

### 🔴 Escalation queue (operator, not loop-runnable)
1. **Twilio → Supabase**: enable Phone provider, paste SID/token/Messaging Service SID (account acquired 2026-07-23). Then OTP UI on `/auth` becomes a 🟢 step next session.
2. **Brevo SMTP → Supabase** + raise email rate limits (in progress 2026-07-23 evening).
3. Google Cloud: OAuth client (sign-in) + Places API key → run `refresh-places.mjs`.
4. VAPID keys + push send-infra decision (cron/server).
5. Analytics decision (Vercel toggle + PostHog account).
6. Apple Developer $99/yr.

## Ticks
(appended by the loop)
