node_modules
.next
out
.env
.env.local
*.log
.DS_Store
.vercel

# Test artifacts
playwright-report/
test-results/
tsconfig.tsbuildinfo

# Generated ingestion candidates (regenerable via scripts/ingest-bars.ts)
scripts/data/

# CEO cycle single-writer lock — runtime state, never committed
ceo/.cycle.lock
ceo/*.tmp-*

# Board audits are a record, not scratch space — but the runtime dir is created on demand
ceo/audits/*.tmp-*
