# T0 revert point — before migration 0059 (2026-08-17)

Recorded **before the apply**, per the T0 live-revenue gate (phase 1).

> ## APPLIED 2026-08-17, attended, on operator instruction
>
> `0059_night_outs_respond_revision.sql` is **live on the serving database**.
> Ledger head `0058` → `0059`, 55 rows, checksum
> `ebc3692d5f33332888961b0d1b92d9595f4c1b80bd23903ca7e9a70dbb73355d`.
> Code half committed at **`9ca82b3`** (feature at `e41a08b`).
>
> Post-apply gate, all five checks passed — see "After the apply" below for what
> each one was. The probe went from **2 RED / 3 PASS** to **6 PASS / 0 FAIL**,
> the concurrency probe still reports **NOT REPRODUCED**, and every installed
> body is byte-identical to the committed file.
>
> The revert instructions below remain live and correct.

- **Goal:** `g-c2d74cdc-f4b6-47c6-a563-abe5f31421b1`
  (replay-safe accept part 2 — monotonic revision CAS closes the ABA hole)
- **Repo:** `D:\harness-worktrees\nb-v8-max-parallel-20260814\.harness-worktrees\aba-revision-20260817\aba-revision`
- **Branch:** `harness/aba-revision-20260817/aba-revision`
- **Known-good commit (the revert point):**
  `d17605d90fe17ce7f18f47bb4252eca1ca26cda3`
  (`fix: [T0] re-sync after a refused response instead of advising a doomed retry`)
- **Worktree state at capture:** clean, branched from `d17605d`

This goal deliberately does **not** work in `core-social`. That worktree sits at
`d17605d`, which is `g-cbc93cbf`'s frozen review candidate, and four other frozen
candidates live in the same workspace — an edit there invalidates a freeze.
`core-social` was verified clean and unchanged at `d17605d` before and after this
work.

## Serving database

`next-bar-staging` (project ref `wqxovhiovgcijmfzxgby`) **is** the serving
database. Ledger head before `0059` is **`0058`** — `0044`–`0054`, `0057`, `0058`
applied, 54 rows total. `0055`/`0056` live on the V8-4 branch and are applied
nowhere, which is why this migration is numbered `0059`; `0059` is unused on
every branch.

Rollback for the database half is **`REVERT-0059-staging-20260817.sql`**, which
restores `respond_night_out` to its exact `0058` body, `get_night_out` to its
`0045` shape, `get_my_night_outs` to its `0053` shape, drops the revision
trigger and its function, and drops the `response_revision` column.

Unlike `0058`'s revert, this one **loses data**: dropping `response_revision`
discards every stored revision. Re-applying `0059` afterwards re-adds the column
at `0` for every row, so a request in flight across a revert-and-reapply would
match again. Do not revert and re-apply while responses are in flight.

Revert coverage is otherwise complete: every applied migration from `0044`
now has a `REVERT-*.sql` in this directory.

## Exposure at the time of capture

**No deployed code calls `respond_night_out`.**
`https://next-bar-two.vercel.app/api/health` reports `sha 6ec5e5d` (2026-08-04);
at that commit the tree contains zero night-out files, its highest migration is
`0019`, and `respond_night_out` appears nowhere. `next-bar.com` does not resolve.

So dropping the 3-argument overload cannot break a live caller, and the ABA
defect this migration closes has no live user-facing exposure today — it is live
in the database, not reachable from the deployed app.

## Pre-apply evidence (phase 2)

`repro-aba-cases-20260817.mjs` against the serving database, PRE-FIX mode
(`repro-aba-cases-prefix.log`): ran 5 of 7, **cases 1 and 2 RED**, cases 3, 4 and
7 PASS, case 5 delegated to `repro-0057-race.mjs`, case 6 not runnable without
the 4-argument overload. The probe detects which overload is live and shapes its
calls accordingly, so the same file is the RED-before and the GREEN-after.

## To roll back, both halves

```
node D:/ClaudeData/.claude/docs/apply-single-migration.mjs \
  D:/ClaudeData/.claude/docs/REVERT-0059-staging-20260817.sql revert

git -C D:/harness-worktrees/nb-v8-max-parallel-20260814/.harness-worktrees/aba-revision-20260817/aba-revision \
  reset --hard d17605d
```

`apply-single-migration.mjs` in `revert` mode restores the prior bodies **and
deletes the `0059` ledger row** in one transaction, so the ledger keeps
describing what is actually installed.

**Rolling back reinstates the ABA hole** — `repro-aba-cases-20260817.mjs` cases 1
and 2 go RED again. That is the point of recording it, not a reason to avoid it.

Note `apply-single-migration.mjs` has `ROOT` hard-coded to the `core-social`
worktree; it reads `.env.local` and `node_modules/pg` from there. That is fine —
it is how it finds the connection string — but it means the file path you pass is
the only thing that selects what runs.

## After the apply (phase 3 — post-deploy smoke, authoritative)

1. `repro-aba-cases-20260817.mjs` in POST-FIX mode: cases 1, 2, 3, 4, 6, 7 must
   all PASS.
2. `repro-0057-race.mjs` unchanged: must still report **NOT REPRODUCED**. A
   change here is a regression in the `0058` fix, not a new finding.
3. `verify-live-defs.mjs`: the live function inventory must still match the
   migration files.
4. Exactly one `respond_night_out` overload live (`pg_proc`) — the probe warns if
   more than one exists.
5. The installed body byte-identical to the committed file, compared with
   `pg_get_functiondef` rather than assumed.

If any of those fail, roll back **first** and diagnose **second**.
