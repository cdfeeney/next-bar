# T0 revert point — migration 0059 (2026-08-17)

Recorded before the apply, per the T0 live-revenue gate (phase 1).

**`0059` was applied on 2026-08-17, attended.** Ledger head `0058` → `0059`,
55 rows, checksum
`ebc3692d5f33332888961b0d1b92d9595f4c1b80bd23903ca7e9a70dbb73355d`.

## What this file is for

**The revert POINT — the known-good commit and the ledger state to return to.**

It deliberately does **not** describe how to run the rollback or what the
rollback costs. `README.md` in this directory is the single source for both. An
earlier version of this file duplicated the procedure and then went stale when
the helper's command signature changed, which is precisely the failure four
review rounds were spent on. One statement, one place.

## The revert point

| | |
|---|---|
| **Goal** | `g-c2d74cdc-f4b6-47c6-a563-abe5f31421b1` |
| **Repo** | `.harness-worktrees/aba-revision-20260817/aba-revision` |
| **Branch** | `harness/aba-revision-20260817/aba-revision` |
| **Known-good commit** | `d17605d90fe17ce7f18f47bb4252eca1ca26cda3` (`fix: [T0] re-sync after a refused response instead of advising a doomed retry`) |
| **Ledger before** | head `0058`, 54 rows |
| **Ledger after** | head `0059`, 55 rows |

Code half of the rollback:

```
git -C .harness-worktrees/aba-revision-20260817/aba-revision reset --hard d17605d
```

Database half: **see `README.md`.**

## Why this goal did not work in core-social

`core-social` sits at `d17605d`, which is `g-cbc93cbf`'s frozen review candidate,
and four other frozen candidates share that workspace — an edit there invalidates
a freeze. This goal was branched into its own worktree instead. `core-social` was
verified clean and unchanged at `d17605d` before and after.

## Exposure at the time of the apply

**No deployed code called `respond_night_out`.**
`https://next-bar-two.vercel.app/api/health` reported `sha 6ec5e5d` (2026-08-04);
at that commit the tree contains zero night-out files, its highest migration is
`0019`, and `respond_night_out` appears nowhere. `next-bar.com` does not resolve.

So dropping the 3-argument overload could not break a live caller, and the ABA
defect had no live user-facing exposure — it was live in the database, not
reachable from the deployed app.

## Pre-apply evidence (phase 2)

`repro-aba-cases-20260817.mjs` against the serving database, PRE-FIX mode: ran
5 of 7, **cases 1 and 2 RED**, cases 3, 4 and 7 PASS, case 5 delegated to
`repro-0057-race.mjs`, case 6 not runnable without the 4-argument overload. The
probe detects which overload is live and shapes its calls accordingly, so the
same file is the RED-before and the GREEN-after.

## Post-apply verification (phase 3 — authoritative)

All five passed on 2026-08-17:

1. The same probe in POST-FIX mode: **6 of 6 runnable cases PASS**, including the
   two that were RED.
2. `repro-0057-race.mjs` unchanged: still **NOT REPRODUCED**, so `0058`'s
   concurrency fix survived the signature change.
3. `verify-live-defs.mjs`: live function inventory matches the migration files.
4. Exactly **one** `respond_night_out` overload live, `(uuid, boolean, text,
   integer)`; the 3-argument form is gone.
5. **Byte-identity**: all four installed bodies sha256-identical to the committed
   file via `pg_get_functiondef`, all `security definer`.

If a future re-verification fails any of these, roll back **first** and diagnose
**second**.
