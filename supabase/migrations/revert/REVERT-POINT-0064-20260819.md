# T0 revert point — migration 0064 (2026-08-19)

Recorded per the T0 gate (phase 1), **before** the apply.

**`0064` was applied to STAGING on 2026-08-20T00:41:19Z (2026-08-19 local),
unattended under goal `g-4e39fc25`.** Ledger head `0059` → `0064`, 55 rows → 56,
checksum `bb48e67e39c1e7c8…` (normalised, per `checksumOfSql`).

Production was NOT touched and is not in scope for this goal.

## What this file is for

**The revert POINT — the known-good commit and the ledger state to return to.**
It does not describe how to run the rollback or what it costs; `README.md` in
this directory is the single source for both.

## The revert point

| | |
|---|---|
| **Goal** | `g-4e39fc25-d21b-49ec-b3b9-aa471d818277` |
| **Repo** | `D:\harness-worktrees\mig-0064` |
| **Branch** | `harness/mig-0064-20260819` |
| **Known-good commit** | `d17394886310041d6240e6ecf3465a85a11bcf19` (`fix: [T1] remove the exploration slot — ranking correctness wins`) |
| **Ledger before** | head `0059_night_outs_respond_revision.sql`, 55 rows |
| **Ledger after** | head `0064_friend_ratings_score.sql`, 56 rows |
| **Target** | staging project ref `wqxovhiovgcijmfzxgby` (`--env staging`, pooler cert verified) |

Code half of the rollback:

```
git -C D:\harness-worktrees\mig-0064 reset --hard d1739488
```

Database half: **see `README.md`.**

## Note for whoever reverts 0059 next

`revert-0059-transaction.sql` refuses while any migration numbered above it sits
in the ledger — which `0064` now does. Revert `0064` first, or that refusal is
the expected answer and not a fault.
