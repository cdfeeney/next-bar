# T0 revert point — migration 0064 (2026-08-19)

Recorded per the T0 gate (phase 1), **before** the apply.

**`0064` was applied to STAGING on 2026-08-20T00:41:19Z (2026-08-19 local),
unattended under goal `g-4e39fc25`.** Ledger head `0059` → `0064`, 55 rows → 56,
checksum `bb48e67e39c1e7c8…` (normalised, per `checksumOfSql`).

> **This page is a HISTORY, read it in order.** Every figure above describes the
> FIRST apply and none of them is the current state. Two attended cycles have
> run since on 2026-08-20, plus one ledger-only write. The ledger is now **58
> rows**, ordered `0059` < `0060` < `0061` < `0064`, and `0064`'s checksum is now
> `aedad98164668b055bf7f185150cd6b28d66818bd9733f8dcc6c8cc46866b8a4`.
> **For the current state read the last two sections — "Second cycle, same day"
> and "0061 recorded".** Those, the migration file, the staging ledger row and
> the pinned constant in `revert-0064-transaction.sql` are what must agree.

Production was NOT touched and is not in scope for this goal.

## What this file is for

**The revert POINT — the known-good commit and the ledger state to return to.**
It does not describe how to run the rollback or what it costs; `README.md` in
this directory is the single source for both.

## The revert point

| | |
|---|---|
| **Goal** | `g-4e39fc25-d21b-49ec-b3b9-aa471d818277` |
| **Repo** | `D:\harness-worktrees\mig-0064` |
| **Branch** | `harness/mig-0064-20260819` |
| **Known-good commit** | `d17394886310041d6240e6ecf3465a85a11bcf19` (`fix: [T1] remove the exploration slot — ranking correctness wins`) |
| **Ledger before** | head `0059_night_outs_respond_revision.sql`, 55 rows |
| **Ledger after** | head `0064_friend_ratings_score.sql`, 56 rows |
| **Target** | staging project ref `wqxovhiovgcijmfzxgby` (`--env staging`, pooler cert verified) |

Code half of the rollback:

```
git -C D:\harness-worktrees\mig-0064 reset --hard d1739488
```

Database half: **see `README.md`.**

## Superseded 2026-08-20 — the ledger moved, the revert point did not

The table above records the state as `0064` was first applied. On 2026-08-20 an
attended, operator-approved reconciliation revised the ledger, because applying
`0064` over the then-head `0059` had jumped `0060_nyc_night_key_sweep.sql` and
stranded it: `apply-migration-set.ts` refuses any file at or below the head, so
`0060` could never have reached staging by the approved path again, and
`npm run check:migrations` was red.

`0060` was proved genuinely unapplied first — absent from the ledger, and all
five functions it rewrites still carried `current_date` with no reference to
`nyc_night_key` — so the ledger was reconciled rather than the file renumbered.

| | |
|---|---|
| **Sequence** | revert `0064` (head → `0059`, 55 rows), then apply `0060` and `0064` as ONE transaction |
| **Ledger then** | 57 rows, head `0064_friend_ratings_score.sql`, ordered `0059` < `0060` < `0064`. It is 58 rows now — see "0061 recorded" below. |
| **Checksums** | `0059` `ebc3692d…`, `0060` `2ba96829…`, `0064` `bb48e67e…` — each matched its file AT THAT POINT. `0064`'s has since moved; see below. |
| **`0064` unchanged** | `md5(pg_get_functiondef(get_friend_ratings))` is `d0b98040eeda1b1c26ea5f009a1f858a` both before the revert and after the reapply |
| **Known-good commit** | unchanged: `d17394886310041d6240e6ecf3465a85a11bcf19` |

## Second cycle, same day — `0064`'s checksum moved to `aedad981…`

A round-2 reviewer found that `0064`'s own trailing rollback comment described a
body swap and omitted the `schema_migrations` DELETE. Correcting those comment
lines changes the file, and therefore its normalised checksum — so the migration
could not simply be edited in place without leaving the applied ledger row
describing a file that no longer existed. It was corrected under a second
attended revert-and-reapply, in the same shape as the first.

| | |
|---|---|
| **Sequence** | revert `0064` (its preconditions still pinned `bb48e67e…`, and passed), correct the comment, recompute with the repository's own `checksumOfSql`, repin the revert script, reapply |
| **`0064` checksum now** | `aedad98164668b055bf7f185150cd6b28d66818bd9733f8dcc6c8cc46866b8a4` |
| **Ledger then** | 57 rows, head `0064`, ordered `0059` < `0060` < `0064`; all three rows equal their files. 58 rows now — see below. |
| **Installed behaviour** | `md5(pg_get_functiondef(get_friend_ratings))` is `d0b98040eeda1b1c26ea5f009a1f858a` — unchanged across BOTH cycles. The edits changed comments, never one byte of what runs. |

**THREE PLACES MUST AGREE, and this document is the one that fell behind.** The
migration file, the `public.schema_migrations` row, and the pinned constant in
`revert-0064-transaction.sql` all carry `aedad981…`. An earlier version of this
section still quoted `bb48e67e…` and claimed it matched — caught by both review
lanes, and exactly the stale-twin failure this directory's one-statement-one-place
rule exists to prevent, reproduced inside the very change that asserted the rule.
`scripts/revert-pin.test.ts` now checks the pin against the migration on every
`npm test`, so the next drift of this kind fails a test rather than a rollback.

**Reverting `0064` now leaves `0061` as the head, not `0059`.** The ledger you
return to is the 57-row one ending at `0061`, and the five night-scoped RPCs
stay on NYC night keys. Reverting `0060` or `0061` as well is a separate
decision and neither has a revert file here.

This paragraph used to restate one of the revert script's preconditions. It no
longer does: those live in the script, next to the code that performs them. The
same restatement drifted three rounds running in `revert/README.md` before it
was deleted there, and a copy in this document would have been the fourth.

Production was NOT touched by any of this and remains out of scope.

## 0061 recorded, 2026-08-20 — ledger-only, no schema change

Migration `0061_ratings_score_integrity.sql` was LIVE but UNRECORDED: it was
applied by hand on 2026-08-19 through `apply-one-migration.mts`, which neither
reads nor writes `public.schema_migrations`. Its row was added on operator
instruction. **Nothing about the schema changed** — one INSERT into the ledger, and one
later UPDATE correcting that row's checksum. No data, no schema.

| | |
|---|---|
| **Ledger now** | 58 rows, head `0064_friend_ratings_score.sql`, ordered `0059` < `0060` < `0061` < `0064` |
| **Proof it was applied** | `0061` backfills rows AND adds a constraint, so both halves were checked: `ratings_score_range` installed with the specified definition, ZERO rows still null with a canonical tier, zero outside the range, and exactly 2 rows carrying the apply fingerprint `2026-08-19 19:18:40.661073+00` at score 9.0 that the pa-score-integrity worktree's `supabase/migrations/revert/REVERT-0061-staging-20260819.sql` records (0061 is not a file on this branch, so neither is its revert artifact) |
| **Checksum recorded** | `d0d89224…`, the text that actually RAN |

**Why the as-applied checksum, and how the drift was avoided.** The file was
edited 34 minutes after it ran (`cd4f9d2` at 19:52:21Z; the apply was
19:18:40Z), so the repository briefly held text that had never executed. The row
first recorded THAT text, `8ed06555…`, which kept ledger and repository in
agreement at the cost of certifying something that never ran — and the ledger's
contract is that its checksum certifies the executed text, comments included.

Rather than choose between literal accuracy and a clean census, the migration
file was RESTORED to the text that ran and the row corrected to match it. Both
now read `d0d89224…` and agree, with no drift for a future census to report.
The comment-only correction that had been added is preserved in that worktree's
`supabase/migrations/revert/README.md`, where it can be improved without
invalidating a certificate.

## Note for whoever reverts 0059 next

`revert-0059-transaction.sql` refuses while any migration numbered above it sits
in the ledger. THREE now do: `0060`, `0061` and `0064`. They come off in
descending order — `0064` first, then `0061`, then `0060` — and only `0064` has
a revert file in this directory; `0060` has none, and `0061`'s lives in the
`pa-score-integrity` worktree. Until all three are gone that refusal is the
expected answer and not a fault.

Reverting `0061` is not a paper exercise: it restores two rows to a null score,
and its own revert file refuses unless they still carry the exact values the
backfill wrote, so a user scoring either bar since makes it refuse rather than
erase real data. Read that file before assuming the chain is mechanical.
